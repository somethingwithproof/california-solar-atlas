{
  "name": "california-solar-atlas",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "data:build": "node scripts/build-data.mjs",
    "data:boundaries": "node scripts/build-boundaries.mjs",
    "test": "node --test tests/*.test.mjs && node scripts/validate-data.mjs"
  },
  "devDependencies": {
    "proj4": "^2.21.0",
    "vite": "^8.2.1"
  }
}
